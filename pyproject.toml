[project]
name = "dpl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Javier", email = "javierreyesperdomo@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
dpl = "dpl:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.65",
]
